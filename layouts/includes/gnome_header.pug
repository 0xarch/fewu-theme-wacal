header.top
    div
        .logo.f_eaj
            if user.logo
                != user.logo
            else
                != fewu.logo()
        nav.index
            ul
                each v in Object.assign(__prov_map||theme.headerObjects,theme.extraNavigations)
                    li
                        if v[2]
                            a(href=file(v[0]))!= Plugin.load_svg(v[1])
                        else
                            a(href=file(v[0]))= i18n(v[1])